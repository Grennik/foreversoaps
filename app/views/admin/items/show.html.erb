<div class="admin-panel">
  <div class="admin-top">
    <h1 class="listing-head"><%= @item.name %></h1>
  </div>
<div class="container listing-table">
  <div class="col-lg-12 col-md-12 col-sm-12 item-window">
    <%= image_tag @item.image_url(:thumb).to_s %><br>
    <%= @item.name %><br>
    <%= @item.description %><br>
    <span>Price: $</span><%= @item.price %>
    <span>Discount Price: $</span><%= @item.discount_price %>
    <span>Stock: </span><%= @item.stock %><br>
    <span>Sub Category: </span><%= @item.sub_category %><br>
    <span>Category Number: </span><%= @item.category_id %><br>
    <%= link_to "Edit", edit_admin_category_item_path(@item.category_id, @item) %>
    <%= link_to "Delete",  admin_category_item_path(@item.category_id, @item), method: :delete, data: {confirm: 'Are you sure?'} %> </td>
  </div>
  <div class="back">
      <%= link_to "Back", admin_category_path(@item.category_id) %>
  </div>
</div>
</div>
